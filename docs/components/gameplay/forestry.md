# Forestry

Forestry adds simple ACE actions for clearing grass, removing bushes and felling trees.

Enable it under Optional Systems. Configure allowed operations, required tool classnames, interaction distance, action durations, grass-cutter class and fallen-tree cleanup delay.

The client performs the progress interaction and the server validates and applies the world change. Use conservative ranges and durations to avoid accidental terrain clearing.
